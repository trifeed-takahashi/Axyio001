apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyPassword 'MINI98'
            storeFile file('/Project/AndroidKey')
            storePassword 'MINI98'
            keyAlias 'key0'
        }
    }
    compileSdkVersion 26
    buildToolsVersion '27.0.1'
    defaultConfig {
        applicationId "jp.co.trifeed.axyio001"
        minSdkVersion 22
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
    }
    productFlavors {
    }
    dexOptions {
    }
}

dependencies {
    implementation 'com.android.support:support-v4:27.0.1'
    compile 'com.android.support:appcompat-v7:27.0.1'
    compile fileTree(include: ['*.jar'], dir: 'libs')
    androidTestCompile('com.android.support.test.espresso:espresso-core:2.2.2', {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
    //compile 'com.android.support:appcompat-v7:24.2.0'
    compile 'com.android.support.constraint:constraint-layout:1.0.2'
    testCompile 'junit:junit:4.12'
    implementation files('/Projects/Axyio001/libs/activation.jar')
    implementation files('/Projects/Axyio001/libs/additionnal.jar')
    implementation files('/Projects/Axyio001/libs/mail.jar')
}
